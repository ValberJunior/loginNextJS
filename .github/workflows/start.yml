name: Deploy NextJS

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Detect package manager
        run: |
          echo "Detect package manager"
      - name: Write Message
        run: |
          echo "Hello World"
      - name: Publish
        run: |
          echo "Deploy Site..."
      - name: Finish
        run: |
          echo "The end *-*"

